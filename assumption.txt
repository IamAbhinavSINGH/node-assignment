i am assuming that your machine have all the dependencies installed with redis-server as well
if they are not install please install them.

having a running redis-server is must , it won't work as expected without it.
